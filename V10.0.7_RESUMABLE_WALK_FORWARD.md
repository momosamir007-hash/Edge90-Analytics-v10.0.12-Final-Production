# Edge90 Analytics v10.0.7 — Resumable Walk-Forward Validation

Adds durable per-fold checkpoints, atomic writes, failure records and aggregation. Interrupted validation resumes from completed chronological folds without recomputing them. The runner preserves temporal isolation: fold tuning/training uses only history available at that fold.
