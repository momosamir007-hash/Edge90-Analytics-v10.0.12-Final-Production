# Edge90 Analytics v10.0.12 — tested core environment snapshot
requests==2.32.5
beautifulsoup4==4.14.3
pandas==2.2.3
numpy==2.3.5
scikit-learn==1.8.0
xgboost==3.1.3
# Streamlit is intentionally constrained in requirements.txt rather than pinned here;
# the target runtime should install a compatible Streamlit 1.x release.
