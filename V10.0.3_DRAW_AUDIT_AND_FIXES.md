# Edge90 Analytics v10.0.3 — Draw Prediction Forensic Audit & Targeted Fixes

## هدف الإصدار
إصلاح مسار التشخيص والقبول المتعلق بالتعادل قبل تغيير أي أوزان أو رفع احتمال التعادل بصورة عشوائية.

## التغييرات
- إضافة `draw_trace` لكل توقع لتسجيل احتمال التعادل عند: Draw Model، Weighted Ensemble، Draw Correction، Calibration، Meta-Learner، والنتيجة النهائية.
- حفظ مسار التعادل داخل سجلات الاختبار لتسهيل التحقيق القابل لإعادة الإنتاج.
- إضافة مقاييس احتمالية للتعادل إلى Walk-Forward: mean draw probability، probability on actual draws، binary draw Brier، binary draw log loss.
- تصحيح معيار Meta-Learner: المقارنة الزمنية أصبحت مع احتمالات الـEnsemble الفعلية المحفوظة في بيانات المعايرة بدلاً من Equal-Weight baseline مصطنع.
- إعادة تصميم بوابة القبول للتفريق بين ضعف جودة احتمال التعادل وبين ندرة اختيار DRAW عبر argmax.
- الاحتفاظ بـargmax draw recall كمؤشر تشخيصي وعدم رفض دوري لمجرد هذا المؤشر وحده.

## مبدأ الأمان
لا توجد زيادة ثابتة أو عشوائية لوزن Draw Model في هذا الإصدار. أي تعديل لاحق للأوزان يجب أن يعتمد على أثر مرحلي مثبت في الـWalk-Forward.

## التحقق
- Python compilation: PASS
- Application import: PASS

## الاختبار المطلوب بعد الإصلاح
إعادة تشغيل Walk-Forward instrumented على Premier League وBundesliga ثم مراجعة `draw_trace` ومقاييس التعادل الاحتمالية قبل قرار الإنتاج.
